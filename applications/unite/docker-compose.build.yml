services:
  portal.unite.net:
    extends:
      file: docker-compose.yml
      service: portal.unite.net
    build: 
      context: ./src
      dockerfile: ./Dockerfile
      args: 
        GITHUB_TOKEN: ${GITHUB_TOKEN}
    image: unite.portal
    
networks: 
  default:
    name: unite
    external: true
