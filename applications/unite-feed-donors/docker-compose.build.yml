services:
  donors-feed:
    extends:
      file: docker-compose.yml
      service: donors-feed
    build: 
      context: ./src
      dockerfile: ./Dockerfile
      args: 
        USER: ${GITHUB_USER}
        TOKEN: ${GITHUB_TOKEN}
    image: unite.donors.feed

networks: 
  default:
    name: unite
    external: true
