services:
  rna-de.analysis.unite.net:
    extends:
      file: docker-compose.yml
      service: rna-de.analysis.unite.net
    build:
      context: ./src
      dockerfile: ./Dockerfile
    image: unite.analysis.rna-de
    volumes:
      - ~/mnt/analysis:/mnt/data:rw

networks:
  default:
    name: unite
    external: true
