services:
  identity-migrations:
    extends:
      file: docker-compose.yml
      service: identity-migrations
    build: 
      context: ./src
      dockerfile: ./Dockerfile
      args: 
        USER: ${GITHUB_USER}
        TOKEN: ${GITHUB_TOKEN}
    image: unite.identity.migrations
    
networks: 
  default:
    name: unite
    external: true
