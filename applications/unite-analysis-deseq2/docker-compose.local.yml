services:
  analysis-deseq2:
    extends:
      file: docker-compose.yml
      service: analysis-deseq2
    volumes:
      - ~/mnt/analysis:/mnt/data:rw

networks:
  default:
    name: unite
    external: true
