services:
  identity:
    extends:
      file: docker-compose.yml
      service: identity
    build: 
      context: ./src
      dockerfile: ./Dockerfile
      args: 
        USER: ${GITHUB_USER}
        TOKEN: ${GITHUB_TOKEN}
    image: unite.identity

networks: 
  default:
    name: unite
    external: true
