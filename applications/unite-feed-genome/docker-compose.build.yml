services:
  genome-feed:
    extends:
      file: docker-compose.yml
      service: genome-feed
    build: 
      context: ./src
      dockerfile: ./Dockerfile
      args: 
        USER: ${GITHUB_USER}
        TOKEN: ${GITHUB_TOKEN}
    image: unite.genome.feed

networks: 
  default:
    name: unite
    external: true
