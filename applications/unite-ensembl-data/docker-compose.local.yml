version: "3.5"
services:
  data.ensembl.unite.net:
    extends: 
      file: docker-compose.yml
      service: unite.ensembl.data
    build: 
      context: ./src
      dockerfile: ./Dockerfile
    image: unite.ensembl.data

networks: 
  default:
    name: unite
    external: true
